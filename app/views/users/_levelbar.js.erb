
const outBar = document.getElementById('outbar');
const inBar = document.getElementById('inbar');

const range = '<%= current_user.calculate_range %>';



  console.log(range)
  if (range === "a") {
    outbar.classList.add("progress2", "progress-moved_a")
    inbar.classList.add("progress-bar2_a")
  } else if (range === "b") {
    outbar.classList.add("progress2", "progress-moved_b")
    inbar.classList.add("progress-bar2_b")
  } else if (range === "c") {
    outbar.classList.add("progress2", "progress-moved_c")
    inbar.classList.add("progress-bar2_c")
  } else {
    outbar.classList.add("progress2", "progress-moved_d")
    inbar.classList.add("progress-bar2_d")
  };



